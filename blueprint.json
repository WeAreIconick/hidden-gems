{
  "landingPage": "/wp-admin/plugins.php",
  "steps": [
    {
      "step": "login",
      "username": "admin",
      "password": "password"
    },
    {
      "step": "writeFile",
      "path": "/wp-content/plugins/test-plugin/test-plugin.php",
      "data": "<?php\n/**\n * Plugin Name: Test Plugin\n * Description: A simple test plugin\n * Version: 1.0.0\n */\n\n// Prevent direct access\nif (!defined('ABSPATH')) {\n    exit;\n}\n\n// Add admin menu\nadd_action('admin_menu', function() {\n    add_options_page(\n        'Test Plugin',\n        'Test Plugin',\n        'manage_options',\n        'test-plugin',\n        function() {\n            echo '<div class=\"wrap\"><h1>Test Plugin</h1><p>This is a test plugin!</p></div>';\n        }\n    );\n});\n?>"
    },
    {
      "step": "runPHP",
      "code": "<?php\nrequire_once('/wordpress/wp-load.php');\n\n// Activate plugin\n$plugin_file = 'test-plugin/test-plugin.php';\nif (file_exists(WP_PLUGIN_DIR . '/' . $plugin_file)) {\n    activate_plugin($plugin_file);\n    error_log('Test Plugin: Activated successfully');\n} else {\n    error_log('Test Plugin: File not found');\n}\n?>"
    }
  ]
}